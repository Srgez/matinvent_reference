_target_: models.suite.MatterGenSuite

model_name: mattergen_base
model_path: null
device: ${device}

sample_cfg:
  batch_size: ${calc:'${eval_size} * 4'}
  num_batches: 1
  sampling_config_path: null

finetune_cfg:
  batch_size: ${eval_size}
  timesteps: 1000
  timestep_start: 0
  timestep_end: null
  timestep_schedule: null
  lr: 1.0e-05
